/*
import { shallowMount } from '@vue/test-utils'
import { describe, it, expect } from 'vitest'
import { RouterLinkStub } from '@vue/test-utils'
import { RouterLink } from 'vue-router'
import NavButton from '../buttons/NavButton.vue'

const TEST_STRING = 'this is a test string'

describe('ButtonRouterLink', () => {
  it(`should transform 'to' into '{ name: to }'`, () => {
    const wrapper = shallowMount(NavButton, {
      props: {
        to: TEST_STRING,
      },
      stubs: {
        RouterLink: RouterLinkStub,
      },
    })

    expect(wrapper.findComponent(RouterLink).props('to')).toEqual(TEST_STRING)
  })
})
*/
